# Legacy

Nenhum HTML antigo foi encontrado nesta pasta no início do projeto (`Casamento Gabriel e Vitoria.html` ou similar não existia). O diretório `/legacy` foi criado preventivamente pela estrutura pedida no brief.

Se um arquivo HTML antigo do convite for encontrado depois, copie-o para cá (mantendo o original também salvo em outro lugar, se possível) para referência histórica. Ele não deve ser editado nem usado como base do novo app — serve apenas como registro do que existia antes.
